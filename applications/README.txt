---+ ClioPatria applications

A _application_ in ClioPatria speech is an HTPP handler and accompanying
predicate that produces a complete HTML page

Applications typically make use of _components_  that are defined in the
directory ../components.
