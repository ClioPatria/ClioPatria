:- module(conf_config, []).

/** <module> Web-based configuration

Add a menu-item _|Admin/Configuration|_ to   the  ClioPatria system menu
that provides a web-based overview  of   the  current  configuration and
allows for enabling  and  disabling   configuration  files  through  the
web-interface.

@see applications(config)
*/

:- use_module(applications(config)).

